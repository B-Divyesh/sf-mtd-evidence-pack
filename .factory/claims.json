[
  {
    "id": "demo-sandbox",
    "claim": "Sample changes are not saved",
    "where": "demo banner and demo guide",
    "test": "npm run test:e2e -- --grep @claim:demo-sandbox",
    "sandbox": "fresh browser context at /demo; change a check, reload, and inspect IndexedDB"
  },
  {
    "id": "sample-content",
    "claim": "The demo loads one quarter with 12 categorised records, three source files, and one open checklist item",
    "where": "landing first-screen result and README sample guide",
    "test": "npm run test:e2e -- --grep @claim:sample-content",
    "sandbox": "fresh browser context at /demo; assert the supplied records, source files, and named open checklist item"
  },
  {
    "id": "csv-import",
    "claim": "Adds valid categorised CSV records without replacing earlier records and rejects invalid dates or amounts",
    "where": "workspace and README",
    "test": "npm run test:e2e -- --grep @claim:csv-import",
    "sandbox": "demo workspace; reject an overflow date and blank or whitespace amount, then import two valid CSV files and assert each earlier record remains"
  },
  {
    "id": "period-integrity",
    "claim": "Rejects records outside the selected period before adding any rows from that file",
    "where": "workspace and README",
    "test": "npm run test:e2e -- --grep @claim:period-integrity",
    "sandbox": "fresh real workspace; import both boundary dates, then try a file containing one outside-period and one inside-period row; assert the second file adds nothing and clears earlier success feedback"
  },
  {
    "id": "source-file-size",
    "claim": "Each source file can be up to 10 MB",
    "where": "workspace source-file control",
    "test": "npm run test:e2e -- --grep @claim:source-file-size",
    "sandbox": "fresh demo workspace; assert a 10 MB file is attached and a 10 MB plus one byte file is rejected without replacing it"
  },
  {
    "id": "encrypted-pack",
    "claim": "Exports a password-protected ZIP with records, a PDF summary, source files, a manifest, and file-change checks (SHA-256) without saving its password",
    "where": "landing, export panel, and README",
    "test": "npm run test:e2e -- --grep @claim:encrypted-pack",
    "sandbox": "demo data; wrong password fails, correct password opens required entries and validates every file-change check"
  },
  {
    "id": "free-evidence-pack",
    "claim": "Import records, maintain the checklist, attach source files, and export an encrypted evidence pack without a licence",
    "where": "landing pricing, workspace, README, and Terms",
    "test": "npm run test:e2e -- --grep @claim:free-evidence-pack",
    "sandbox": "fresh context begins at /demo, enters an unlicensed real workspace, imports a record, adds a checklist item, attaches a file, and downloads an encrypted evidence pack"
  },
  {
    "id": "local-only",
    "claim": "Records entered from the demo flow stay in local browser storage",
    "where": "landing and workspace",
    "test": "npm run test:e2e -- --grep @claim:local-only",
    "sandbox": "start at /demo, enter the real workspace, import a record, inspect IndexedDB, and assert only the product origin was requested"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after your first visit",
    "where": "landing and README",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context at /demo; wait for every production chunk to cache, go offline, reload, and download a complete encrypted sample pack"
  },
  {
    "id": "custom-checklist",
    "claim": "Maintains a user-defined period checklist",
    "where": "workspace and README",
    "test": "npm run test:e2e -- --grep @claim:custom-checklist",
    "sandbox": "fresh browser context at /demo; start a real unlicensed workspace, add a custom check, reload, and assert it remains"
  },
  {
    "id": "readiness",
    "claim": "See what is missing before export",
    "where": "landing preview and export panel",
    "test": "npm run test:e2e -- --grep @claim:readiness",
    "sandbox": "fresh demo; assert the one named open checklist item is shown, complete it, and assert the ready state"
  },
  {
    "id": "standalone-install",
    "claim": "If your browser offers Install, use it to open the tool in its own window",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:standalone-install",
    "sandbox": "fresh landing page; inspect the shipped manifest display mode, start URL, and 192/512 icons"
  },
  {
    "id": "paid-license",
    "claim": "A verified licence enables saved cover notes, and verification sends its token to the Sociobot billing API",
    "where": "landing pricing, workspace, Privacy page, and README",
    "test": "npm run test:e2e -- --grep @claim:paid-license",
    "sandbox": "fresh context starts at /demo, enters the real workspace with a recorded valid verify response, and asserts the exact API request and enabled cover-note field"
  },
  {
    "id": "checkout-unavailable",
    "claim": "New licences are not currently available",
    "where": "landing pricing card and README",
    "test": "npm run test:e2e -- --grep @claim:checkout-unavailable",
    "sandbox": "fresh browser context visits every public product route and asserts there is no price, checkout URL, or purchase action while licence restore remains available"
  },
  {
    "id": "no-tax-submission",
    "claim": "It does not submit tax returns",
    "where": "landing limitation section",
    "test": "npm run test:e2e -- --grep @claim:no-tax-submission",
    "sandbox": "fresh context begins at /demo, completes a sample export, enters the real workspace, and records that no tax-submission control or HMRC request exists"
  },
  {
    "id": "artwork-provenance",
    "claim": "Hero artwork was generated for this product",
    "where": "landing and 404 footers",
    "test": "npm run test:e2e -- --grep @claim:artwork-provenance",
    "sandbox": "fresh context begins at /demo and checks the public landing and designed 404 footer disclosure"
  },
  {
    "id": "node-runtime",
    "claim": "Requires Node.js 20.19+ or 22.12+",
    "where": "README and package metadata",
    "test": "npm run test:e2e -- --grep @claim:node-runtime",
    "sandbox": "fresh clone; use Node 20.19.0, the lower supported version, to run npm ci and npm run build"
  }
]
